// LocationsListener was removed from the app and the route/nav link was deleted.
// This file remains as a placeholder to avoid import errors in case other
// references exist. The component renders null.
import React from 'react';
const LocationsListener = () => null;
export default LocationsListener;
